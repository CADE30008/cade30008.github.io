# dependencies
node_modules/
.venv/
# build outputs
.live/
site/
.cache/
docs/slides/

# Blackboard packages, built by `npm run bb` from diagnostics/*.yaml.
# The generator and the README are tracked; its output is not, for the same
# reason site/ is not.
diagnostics/blackboard/*.zip
diagnostics/blackboard/*.txt
docs/downloads/
# OS
.DS_Store
# private material: assessment briefs, rubrics, last year's work, licensed resources
private/*
!private/README.md
__pycache__/
models/envelope-matlab.json
